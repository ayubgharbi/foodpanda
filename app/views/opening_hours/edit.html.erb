<div class="jumbotron" id="jumbotron1">
</div>
<div class="container">
	<div class="row">
		<div class="col-md-6 offset-md-3">
			<h1>Edit Opening Hour for <%= @restaurant.name %></h1>

			<%= render 'form' %>

			<%= link_to 'Back', @restaurant_opening_hours_path(@opening_hour.restaurant_id) %>
			<%= link_to 'Show', @restaurant_opening_hour_path(@opening_hour.restaurant_id, @opening_hour.id) %>
		</div>
	</div>
</div>